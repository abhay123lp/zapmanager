#include "FileAttributes.h"
using namespace shaman;

/* bool FileAttributes::operator< ( const FileAttributes &rhs ) const {
    return ((this->name.CmpNoCase( rhs.name ) < 0) && (this->hash != rhs.hash));
}
*/
